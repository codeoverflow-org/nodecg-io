{
    "name": "nodecg-io",
    "private": true,
    "scripts": {
        "postinstall": "cd nodecg-io-core && npm link && cd ../nodecg-io-twitch && npm link nodecg-io-core && npm link && cd ../sample && npm link nodecg-io-twitch nodecg-io-core && npm i",
        "build": "tsc",
        "watch": "tsc -w"
    },
    "devDependencies": {
        "@types/node": "^14.0.4",
        "typescript": "^3.9.3"
    }
}
