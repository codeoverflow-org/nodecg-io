{
    "name": "nodecg-io",
    "private": true,
    "scripts": {
        "postinstall": "lerna bootstrap",
        "build": "lerna run build --parallel",
        "watch": "lerna run --parallel watch"
    },
    "devDependencies": {
        "lerna": "^3.22.1"
    }
}
