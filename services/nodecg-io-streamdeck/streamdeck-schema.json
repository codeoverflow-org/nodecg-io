{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "device": {
            "type": "string",
            "description": "The device id of the streamdeck to use. Use 'default' to auto detect this."
        }
    },
    "required": ["device"]
}
