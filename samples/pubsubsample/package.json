{
    "name": "pubsubsample",
    "version": "0.1.0",
    "private": true,
    "nodecg": {
        "compatibleRange": "^1.1.1",
        "bundleDependencies": {
            "nodecg-io-twitch-pubsub": "^0.1.0"
        }
    },
    "scripts": {
        "build": "tsc",
        "watch": "tsc -w",
        "clean": "tsc -b --clean"
    },
    "license": "MIT",
    "dependencies": {
        "@types/node": "^14.14.13",
        "nodecg-io-twitch-pubsub": "^0.1.0",
        "nodecg-io-core": "^0.1.0",
        "nodecg": "^1.7.4",
        "typescript": "^4.1.3"
    }
}
