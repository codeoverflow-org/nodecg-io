import { SQLConfig } from "./index";

export class SQLClient {
    constructor(_: SQLConfig) {
        // TODO: Implement
    }

    static createClient(config: SQLConfig): SQLClient {
        // TODO: Implement
        return new SQLClient(config);
    }
}
