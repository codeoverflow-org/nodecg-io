import { TemplateConfig } from "./index";

export class TemplateClient {
    constructor(_: TemplateConfig) {
        // TODO: Implement
    }

    static createClient(config: TemplateConfig): TemplateClient {
        // TODO: Implement
        return new TemplateClient(config);
    }
}
