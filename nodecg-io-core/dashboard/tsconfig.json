{
	"compilerOptions": {
		"target": "ES2015",

		"lib": ["ES2015", "dom"],
		"module": "ES2015"
	},
	"extends": "../../tsconfig.json",
	"exclude": [] // Removes excluding of dashboard of the tsconfig in the repo root
}